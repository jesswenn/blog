{{-- @extends('layouts.app')
        
@section('content')
    <h1>Services</h1> --}}

   {{--  Counting at least one value in the array
        in pages controller method --}}
    {{-- @if(count($services) > 0) --}}

        {{-- Loop trow the sevices array --}}
{{--         <ul class="list-group">
            @foreach ($services as $service)               
                <li class="list-group-item">{{ $service }}</li>
            @endforeach
        </ul>
    @endif
@endsection --}}

